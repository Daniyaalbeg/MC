import * as volunteerActions from '../Actions/volunteerActions';

const initalVolunteerDashState = {
  loading: false,
  hasErrors: false,
  success: false,
}

function createVolunteerDashReducer( state = initalVolunteerDashState, action) {
  // switch (action.type) {
  //   case volunteerActions:

  //   default:
  //     return state
  // }
}